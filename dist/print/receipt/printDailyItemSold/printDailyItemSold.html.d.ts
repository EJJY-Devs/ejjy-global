import { PrintDailyItemSold } from './types';
export declare const printDailyItemSoldHtml: ({ dailyItemSoldSummary, branch, branchMachine, user, isPdf, reportDate, }: PrintDailyItemSold) => string | undefined;
