import { Product, SiteSettings } from '../../../types';
import { PaperSettings } from './types';
export declare const printProductPriceTagNative: (product: Product, _siteSettings: SiteSettings, _paperSettings: PaperSettings) => string[];
