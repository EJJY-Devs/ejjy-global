import { BranchMachine, SiteSettings, Transaction, User } from '../../types';
export declare const printBirReportSC: (transactions: Transaction[], siteSettings: SiteSettings, user: User, branchMachine?: BranchMachine) => string;
