import { GeneratedByUser, SiteSettings, XReadReport } from '../../types';
export declare const createXReadTxt: (report: XReadReport, siteSettings: SiteSettings, user: GeneratedByUser, returnContent?: boolean) => string | null;
