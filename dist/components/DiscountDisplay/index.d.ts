import React from 'react';
import { DiscountOption } from '../../types';
type Props = {
    discountOption: DiscountOption;
    overallDiscount: number;
};
export declare const DiscountDisplay: ({ discountOption, overallDiscount }: Props) => React.JSX.Element;
export {};
