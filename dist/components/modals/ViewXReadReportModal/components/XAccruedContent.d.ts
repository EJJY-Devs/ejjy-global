import React from 'react';
import { XReadReport } from '../../../../types';
type Props = {
    report: XReadReport;
};
export declare const XAccruedContent: ({ report }: Props) => React.JSX.Element;
export {};
