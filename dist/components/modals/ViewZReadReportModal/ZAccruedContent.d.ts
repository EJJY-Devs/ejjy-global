import React from 'react';
import { ZReadReport } from '../../../types';
type Props = {
    report: ZReadReport;
};
export declare const ZAccruedContent: ({ report }: Props) => React.JSX.Element;
export {};
