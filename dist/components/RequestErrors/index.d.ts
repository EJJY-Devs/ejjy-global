import React from 'react';
type Props = {
    className?: string;
    errors: string[];
    withSpaceTop?: boolean;
    withSpaceBottom?: boolean;
};
export declare const RequestErrors: ({ className, errors, withSpaceTop, withSpaceBottom, }: Props) => React.JSX.Element;
export {};
